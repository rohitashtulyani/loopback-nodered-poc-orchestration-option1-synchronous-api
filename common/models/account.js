module.exports = function(Account) {

	/*Account.observe('before save', function (ctx, callback) {
		console.log("Inside before accout save observer :::::::: ");
		if(ctx.instance.accountNumber === "67890"){
			return callback(new Error(" Account Number is not valid."));
		} else {
			return callback();
		}
	
	});

	Account.observe('after save', function (ctx, callback) {
		console.log("Inside after accout save observer :::::::: ctx.instance is  ", ctx.instance);
		
			return callback();
	
	
	});*/
};
